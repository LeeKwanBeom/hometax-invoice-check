---
name: hometax-invoice-check
description: 홈택스 매입 전자세금계산서·전자계산서 목록을 받아, 거래처가 세금계산서를 빠뜨리고 안 보낸 곳이 있는지 점검해 엑셀 리포트를 만든다. "세금계산서 점검", "매입 세금계산서 확인", "미수취 확인", "계산서 안 온 곳", "세금계산서 누락", "홈택스 매입 점검", "부가세 자료 점검", "세금계산서 스킬" 같은 말이 나오거나, 홈택스 매입 목록 xls(매입전자세금계산서목록_*.xls / 매입전자계산서목록_*.xls)가 업로드되면 반드시 이 스킬을 사용할 것. 거래처별 월별 수취 매트릭스, 미수취 의심 목록, 발급기한 임박 확인, 거래처 목록(vendors) 관리에 적용된다.
---

# 매입 세금계산서 수취 점검

홈택스에서 받은 매입 전자(세금)계산서 목록으로 **"어느 거래처가 안 보냈는지"**
를 찾아 엑셀 리포트를 만든다. 월 1~2회, 주로 월초에 실행한다.

**코드·설정·거래처 목록·실행 이력은 전부 GitHub 저장소에 있다.**
이 파일은 그 저장소를 어떻게 쓰는지에 대한 설명서다.
컨테이너가 초기화되어도, 다른 컴퓨터에서 실행해도 매번 최신본을 받아오므로
로컬 상태에 의존하지 않는다.

- 저장소: `LeeKwanBeom/hometax-invoice-check` (Public)
- 읽기는 토큰 없이 된다. **쓰기(코드·거래처 목록·실행 이력 반영)에만 토큰이 필요하다.**
- 토큰은 **이 파일이나 스크립트에 저장하지 않는다.** 매번 대화창에서 받는다.

## 가장 중요한 원칙 세 가지

**1. 판정 대상은 업로드 데이터가 아니라 `config/vendors.json` 이다.**
데이터를 groupby 해서 리포트를 만들면 "한 건도 없는 거래처"를 원리적으로 못 잡는다.
가장 중요한 점검 항목이 통째로 빠진다.

**2. 기준일·월 범위는 실행 시점에서 계산한다.**
고정값을 쓰면 다음 달부터 틀린다. 10월에 돌릴 때 10월 데이터가 오류 없이
사라지는 게 가장 위험한 형태다.

**3. 발급기한이 안 지난 달은 미수취로 확정하지 않는다.**
세금계산서 발급기한은 작성월 다음 달의 정해진 날짜다(`config` 의 `grace.deadline_day`).
월초에 돌리면 전월분이 아직
안 온 게 정상이다. 유예가 없으면 매월 거래처 전부가 오탐으로 잡힌다.

임계값·색상·시트명은 **전부 `config/check-config.json` 이 유일한 출처다.**
스크립트나 이 문서에 같은 값을 다시 적지 말 것.

---

## 작업 순서

작업 디렉토리는 항상 `/home/claude/` 하위를 쓴다.

### 1단계. 저장소에서 코드 받기

```bash
mkdir -p /home/claude/hometax && cd /home/claude/hometax
curl -sL https://codeload.github.com/LeeKwanBeom/hometax-invoice-check/tar.gz/refs/heads/main | tar xz --strip-components=1
python3 -c "import xlrd" 2>/dev/null || pip install xlrd --break-system-packages -q
ls
```

`xlrd` 가 없으면 홈택스 `.xls` 를 한 줄도 못 읽는다. 컨테이너에 없을 때가 많으니
위 한 줄로 먼저 확인한다.

`config/`, `references/`, `scripts/`, `state/`, `tests/` 가 보이면 성공이다.
안 보이면 저장소가 비어 있거나 비공개인 것이니 사용자에게 알린다.

**`raw.githubusercontent.com` 으로 받지 말 것.** 5분간 캐시되어서,
push 직후 다시 받으면 옛 파일이 내려온다. codeload 는 캐시되지 않고
요청 한 번으로 저장소 전체를 받는다.

대화 도중 다시 받아야 하면 `python3 sync.py .` 를 쓴다. 같은 tarball 방식이다.

| 옵션 | 언제 |
|---|---|
| `--ref <브랜치\|커밋SHA>` | 특정 커밋 시점 코드로 되돌려 확인. 기본 `main` |
| `--token <토큰>` | 저장소를 비공개로 바꿨을 때만. 지금은 Public 이라 불필요 |

판정 논리나 서식 규격을 이해해야 하면 `references/` 안의 문서를 읽는다.
특히 등급 판정을 손보기 전에는 `references/judgment-rules.md` 를 먼저 읽는다.

`audit/last-audit.md` 가 있으면 **함께 읽는다.** 직전 점검 기준선이고, 알려진
미해결 결함과 "다음 점검에서 먼저 볼 것" 이 적혀 있다. 결과가 이상해 보이거나
스킬을 고치기 전에 먼저 보면, 이미 알려진 문제를 다시 찾느라 시간을 쓰지 않는다.

### 2단계. 파일 확인

사용자에게 받아야 할 것:

- 홈택스 → 조회/발급 → 전자세금계산서 → **매입** 목록조회 → 엑셀 다운로드
- 홈택스 → 조회/발급 → 전자계산서 → **매입** 목록조회 → 엑셀 다운로드
- 기간은 **당해 1월 1일 ~ 오늘**. 홈택스는 3개월씩만 조회되므로 보통 파일이 여러 개다
- **1월에 실행할 때만 전년 12월 1일부터 받는다.** 12월분 발급기한이 익년 1월이라
  점검 기간에 전년 12월이 자동으로 들어간다. 당해 1/1 부터만 받으면
  `check_input.py` 가 "전년 12월 데이터가 통째로 없음" FAIL 을 내고 거기서 멈춘다

경로를 추측하지 말고 `view /mnt/user-data/uploads` 로 실제 파일명을 확인한다.

두 종류 중 하나만 있으면 그대로 진행하되, 빠진 쪽 때문에 무엇이 안 보이는지 알린다
(계산서가 없으면 농축수산물 등 면세 매입이 통째로 빠진다).

### 3단계. 입력 검증 — 건너뛰지 말 것

```bash
cd /home/claude/hometax
python3 scripts/check_input.py /mnt/user-data/uploads
```

과거 시점을 재현할 때는 `--as-of YYYY-MM-DD` 를 붙인다. **4단계에서 `--as-of` 를
쓸 거면 여기에도 같은 날짜를 준다.** 안 그러면 검증은 오늘 기준, 리포트는 과거
기준이 되어 기간 구간 판정이 어긋난다.

**FAIL 이 하나라도 나오면 리포트를 만들지 말고 먼저 사용자에게 알린다.**

특히 "기간 구간 누락"은 그대로 진행하면 전 거래처가 그 달 미수취로 잘못 나온다.
홈택스는 3개월씩만 조회되므로 한 구간을 빼먹기 쉽다.
정말 거래가 없던 달일 수도 있으니 **사용자 확인 후에만** 넘어간다.

WARN 은 진행해도 되지만 리포트를 줄 때 함께 언급한다.

### 4단계. 리포트 생성

```bash
python3 scripts/build_report.py --uploads /mnt/user-data/uploads --out /mnt/user-data/outputs
```

| 옵션 | 언제 |
|---|---|
| `--as-of YYYY-MM-DD` | 과거 시점 재현. 미지정시 오늘. 그 날짜 이후 데이터는 잘라낸다 |
| `--strict` | 지난 달까지 유예 없이 전부 미수취 판정. **부가세 신고 직전(1월·7월) 최종 점검용** |
| `--no-state` | 직전 실행과 비교하지 않음 |

`--strict` 라도 **실행 당월은 유예**다. 아직 끝나지 않은 달이라 미수취로 확정할
근거가 없고, 확정하면 매번 전 거래처가 결번으로 잡힌다.

**1월에 실행하면 전년 12월이 점검 기간에 자동으로 들어간다.** 12월분 기한이
익년 1월이라, 넣지 않으면 12월은 영영 판정되지 않는다. 매트릭스에 `전년 12월`
열로 나온다.

### 5단계. 산출물 검증 — 건너뛰지 말 것

```bash
python3 scripts/validate.py /mnt/user-data/outputs/<생성된파일>.xlsx --uploads /mnt/user-data/uploads
```

**4단계에서 `--as-of` 나 `--strict` 를 줬으면 여기에도 똑같이 준다.**
안 주면 검증기가 다른 기준으로 판단해서 멀쩡한 산출물에 FAIL 을 낸다
(strict 산출물은 '기한 전'이 0건인 게 정상인데, 그걸 유예 로직 고장으로 오판한다).

```bash
python3 scripts/validate.py <파일>.xlsx --uploads /mnt/user-data/uploads --strict
```

**FAIL 이 있으면 산출물을 사용자에게 주지 않는다.**
검증기는 0건 매칭을 PASS 로 넘기지 않는다. "검사 대상을 못 찾음"도 FAIL 이다.
승인번호가 중복되면(같은 구간 파일을 두 번 올린 경우) 여기서도 FAIL 이 난다.

### 6단계. 결과 전달

`present_files` 로 xlsx 를 주고, 채팅에는 **확인 필요 항목만** 표로 요약한다.
단발·비정기와 정상(오탐)까지 채팅에 옮기면 정작 봐야 할 게 묻힌다.

요약에 반드시 포함할 것:

- 확인 필요 몇 건 / 기한 전 몇 건
- `상태` 가 "계속 (N개월)" 인 항목 — 여러 달 미해결이라 우선순위가 높다
- "해결" 로 뜬 항목 — 지난달 요청이 처리된 것. **한 번만 표시되고 사라진다**
- 매출분 제외 건수, 상계쌍 수

조치 목록 시트(`sheets.missing`)의 등급은 여섯 가지다. 채팅에 옮기는 건 사실상 위의 두 개뿐이다.

| 등급 | 뜻 | 조치 |
|---|---|---|
| `확인 필요` | 기한이 지났는데 안 왔거나 발행 후 전액취소됨 | **거래처에 발행 요청** |
| `기한 전` | 발급기한이 아직 남음 | 기다린다 |
| `단발·비정기` | 정기 거래가 아니라 결번에 의미가 없음 | 무시 |
| `정상` | 결번 없음 | 무시 |
| `거래 종료` | `vendors.json` 에 `until` 로 등록됨 — 점검 대상 아님 | 무시. 아직 거래 중인데 여기 뜨면 `until` 을 지운다 |
| `해결` | 직전 실행의 `확인 필요` 가 이번엔 정상 | 확인만. 다음 실행부터 안 보인다 |

판정 논리를 손보기 전에는 `references/judgment-rules.md` 를 먼저 읽는다.

### 7단계. 저장소에 반영 (조건부)

**대화 안에서 고친 것은 컨테이너와 함께 사라진다.** 저장소에 올려야만 남는다.
안 올리면 다음 실행 때 `sync.py` 가 옛 버전으로 덮어써서 수정이 없어진다.

`push.py` 는 세 묶음으로 나눠 올린다.

| 묶음 | 대상 | 언제 |
|---|---|---|
| `state` | `state/last-run.json` | 실행할 때마다 (기본 포함) |
| `vendors` | `config/vendors.json` | 거래처 추가·수정 (기본 포함) |
| `code` | `SKILL.md`, `install/SKILL.md`, `scripts/`, `config/check-config.json`, `references/`, `tests/`, `audit/`, `sync.py`, `README.md` | **스킬 자체를 고쳤을 때. `--code` 필수** |

`tests/`·`audit/`·`install/` 은 파일명을 적어두지 않고 디렉토리째 훑는다. 새 파일을
만들어도 목록을 고칠 필요가 없다. 없는 디렉토리는 그냥 건너뛴다.

`SKILL.md` 도 저장소에 있고 `--code` 로 함께 올라간다.

| 위치 | 무엇 | 어떻게 |
|---|---|---|
| 저장소 `SKILL.md` | **정본 설명서** | `push.py --code` |
| 저장소 `install/SKILL.md` | 설치용 부트스트랩 문구 | `push.py --code` |
| `/mnt/skills/plugins/hometax-invoice-check/SKILL.md` | 자동으로 읽히는 파일 | **직접 쓰지 말 것** (아래) |

**설치 경로에 직접 쓰지 않는다.** 쓰기는 되지만 **세션 간 보존되지 않는다.**
이 경로는 매 세션 패키지에서 다시 풀린다(2026-09-06 실측: 전날 덮어쓴
SKILL.md 가 옛 버전으로 되돌아가 있었고, 같이 둔 `audit/` 파일도 사라졌다).
거기 쓰는 수정은 이번 대화에서만 보이고 다음 세션에 없어진다.

설치본을 바꿔야 하면 저장소 `install/SKILL.md` 를 고쳐 push 한 뒤,
**사용자가 Claude 설정에서 스킬을 재업로드해야 한다.** 이건 대신 해줄 수 없다.
그래서 설치본은 절차를 담지 않는 얇은 부트스트랩으로 유지한다 — 바뀔 일이
없어야 재업로드도 필요 없다.

토큰이 아직 없으면:

> 저장소에 반영하려면 GitHub 토큰이 필요해요. 대화창에 붙여넣어 주세요.
> (Settings → Developer settings → Fine-grained tokens, hometax-invoice-check 저장소에
> Contents: Read and write 권한)

```bash
python3 scripts/push.py <토큰>                  # 이력·거래처만
python3 scripts/push.py <토큰> --code           # 코드까지 고쳤을 때
python3 scripts/push.py <토큰> --code --dry-run # 뭐가 바뀌는지 먼저 확인
```

| 옵션 | 언제 |
|---|---|
| `--code` | 코드·문서·테스트까지. 안 붙이면 `state`+`vendors` 만 |
| `--dry-run` | 비교만 하고 올리지 않음. 확신이 없으면 먼저 이걸로 본다 |
| `--only state\|vendors\|code` | 한 묶음만. 예: 거래처만 고쳤을 때 `--only vendors` |
| `--message "..."` | 커밋 메시지 지정. 미지정시 바뀐 파일명으로 자동 생성 |
| `--skip-tests` | 테스트 게이트를 건너뛴다. **쓰지 않는다** |
| `--force` | 실행 이력이 뒤로 가도 강행. **쓰지 않는다** (아래 참고) |
| `--add-vendor <번호...>` | vendors.json 에 거래처를 추가하고 바로 올린다 |
| `--ignore-vendor <번호...>` | config `ignore_suppliers` 에 넣어 '추가 권장' 재권유를 끈다 |
| `--cycle <값>` | `--add-vendor` 로 넣을 cycle. 기본 `매월` |

### 대상외 목록에서 바로 등록·제외하기

리포트 끝에 찍히는 '추가 권장' 목록의 사업자번호를 그대로 붙이면 된다.
시트를 열어 손으로 옮겨 적을 필요가 없다.

```
python3 scripts/push.py <토큰> --add-vendor 212-04-59010
python3 scripts/push.py <토큰> --ignore-vendor 212-04-59010
```

- 하이픈은 있어도 되고 없어도 된다. 이미 등록·제외된 번호는 건너뛴다.
- `--add-vendor` 는 `name` 을 비워 둔다. 리포트를 다시 만들면 자료에서 채워진다.
- 목록만 고치고 끝나지 않는다. 고친 파일이 바로 올라간다.
- `--dry-run` 을 같이 쓰면 **무엇이 추가·제외될지 화면에만 찍고 파일은 안 고친다.**
  올리지도 않는다. 번호를 제대로 붙였는지 먼저 볼 때 쓴다.
- 둘을 같이 쓸 수는 없다. **추가할지 뺄지 먼저 정하고 한 번에 하나만** 쓴다.

**의도적으로 뺀 거래처는 `--ignore-vendor` 를 쓴다.** vendors.json 에서 지우기만 하면
'추가 권장' 으로 매 회차 다시 올라와 같은 판단을 반복하게 된다.

`push.py` 는 `state/last-run.json` 이 원격보다 옛것이면 스스로 멈춘다.
sync 하지 않은 폴더에서 올려 원격 이력을 지우는 사고를 막는 장치다.
기본 대응은 `--force` 가 아니라 **`sync.py` 로 최신본을 받은 폴더에서 다시
실행**하는 것이다. 강행하면 '신규/계속/해결' 비교 기준이 그만큼 사라진다.

### 가드가 걸렸을 때 밟는 3단계

받아서 다시 돌려도 이력이 줄어드는 경우가 있다. **거래처를 의도적으로 뺐을 때**가
그렇다. 그때는 사고가 아니라 정상적인 감소이므로 아래를 순서대로 확인한다.
2026-09 에 실제로 밟은 절차다.

1. **원격 state 를 받아서 무엇이 줄었는지 이름으로 확인한다.**
   `sync.py` 로 받은 뒤 `state/last-run.json` 의 `flagged` 목록을 이번 산출물과
   비교한다. 줄어든 항목이 **내가 이번 회차에 vendors.json 에서 뺀 곳**과
   일치해야 한다.
2. **일치하지 않는 항목이 하나라도 있으면 멈춘다.** 그건 sync 누락이거나
   자료 기간이 잘못된 것이다. `--force` 를 쓰면 안 된다.
3. **전부 일치하면** 감소가 의도된 것이므로 `--force` 가 정당하다.
   커밋 메시지에 무엇을 왜 뺐는지 남긴다
   (예: `--message "유성빌딩 제외 — 4~8월 전액취소, 거래 종료 확인"`).

바꿔 말하면 `--force` 는 **줄어든 항목 전부를 이름으로 설명할 수 있을 때만**
정당하다. 하나라도 설명이 안 되면 강행하지 않는다.

올리기 전에 **바뀐 파일과 줄 수를 먼저 출력한다.** 예상과 다르면 그 시점에 멈춘다.

`--code` 는 `tests/test_edge_cases.py` 를 먼저 돌리고 **실패하면 올리지 않는다.**
테스트가 실패하면 그 내용을 사용자에게 그대로 전달하고, 고친 뒤 다시 시도한다.
`--skip-tests` 가 있지만 쓰지 않는다.

테스트 픽스처(`tests/fixtures/`)는 1단계에서 이미 함께 받아진다.

**사용자가 토큰 주기를 원치 않으면 그대로 넘어간다.** 리포트 자체는 이미 나왔고,
잃는 건 "다음 실행 때의 신규/계속 비교"뿐이다.

> 토큰 없이 진행할게요. 다만 이번 이력이 저장소에 안 남아서, 다음에 돌릴 때
> '신규/계속' 비교가 이번 것 기준으로는 안 됩니다.

다만 **코드를 고쳤는데 토큰이 없으면 그냥 넘어가지 않는다.** 고친 파일을
`present_files` 로 건네주고 저장소에 직접 올리도록 안내한다. 안 그러면 수정이 사라진다.

---

## 거래처 목록 관리

`config/vendors.json` 이 점검 대상 마스터다. 신규 거래처가 생기면 **여기에만** 추가한다.

```json
{"biz_no": "0000000000", "name": "예시상사", "cycle": "매월",
 "since": "2026-03", "until": null, "note": "임대료"}
```

| 필드 | 설명 |
|---|---|
| `biz_no` | 하이픈 없이 숫자 10자리. 체크섬은 check_input.py 가 검증 |
| `cycle` | `매월` / `분기` / `비정기`. **판정을 면제하는 스위치가 아니다** — 아래 참고 |
| `since` `until` | 선택. 개시·종료한 곳. 그 밖의 달은 결번으로 세지 않는다. `until` 이 작년이면 올해는 통째로 점검 대상에서 빠진다 |
| `note` | 리포트 '비고' 열에 그대로 나온다 |

리포트를 준 뒤, **대상외공급자 시트에서 "추가 권장" 이 붙은 곳이 있으면
vendors.json 에 추가할지 사용자에게 물어본다.** 이게 목록을 살아있게 유지하는
유일한 방법이다. 추가했으면 7단계로 저장소에 올린다.
`build_report.py` 가 실행 끝에 같은 목록을 콘솔에도 찍어주므로 놓치지 않는다.
기준(수취 월수·금액)은 `config` 의 `thresholds.unlisted_recommend_*` 에 있다.

`check_input.py` 는 **cycle 이 실제 수취 패턴과 어긋나는 거래처**도 WARN 으로
알려준다(예: `비정기` 로 적혀 있는데 다섯 달 연속 수취). 판정에는 영향이 없지만,
맞으면 vendors.json 을 고쳐야 다음 달부터 제대로 잡힌다. 사용자에게 확인한다.

### `비정기` 라벨의 범위 — 등급이 안 바뀌어도 정상이다

`cycle` 을 `비정기` 로 바꿔도 **등급이 그대로일 수 있다.** 버그가 아니라 설계다.
사용자가 "이 업체는 가끔 거래한다"고 알려줘 라벨을 바꿨는데 `확인 필요` 가
그대로였던 적이 있다(2026-09). 그때 이렇게 설명한다.

| 경로 | 언제 타나 | `비정기` 를 |
|---|---|---|
| A (결번·전액취소·수취 0) | 결번이 실제로 있을 때 | **안 본다** — 수취 월수가 `thresholds.monthly_min_months` 이상이면 정기로 판정 |
| C (최종수취 후 경과) | A 에 안 걸렸을 때 | **본다** — `단발·비정기` 로 내린다 |

A 가 라벨을 무시하는 것은 **오등록에 대한 안전장치**다. 라벨 하나로 정기 판정을
면제하면, 잘못 붙인 라벨 때문에 진짜 미수취를 통째로 놓친다.

그러므로 `비정기` 는 **"경과일로 재촉하지 말라"는 C 경로 힌트**이지
"결번을 못 본 척하라"는 뜻이 아니다. 결번이 실제로 있는데 등급을 내리고 싶다면
라벨이 아니라 `until`(거래 종료) 이나 `since`(개시 시점) 로 기간을 좁혀야 한다.

사업자번호가 바뀐 거래처는 구 번호에 `until`, 신 번호에 `since` 를 넣는다.
`check_input.py` 의 "같은 상호인데 사업자번호가 다름" 경고가 이런 케이스를 찾아준다.

---

## 자주 나오는 상황

**"8월분이 전부 미수취로 나왔어요"**
→ 발급기한 전이라 정상이다. 등급이 `기한 전`이면 기다리면 된다.
`확인 필요`로 떴다면 기한이 지난 것이다.

**"작년에 거래 끊은 곳이 계속 잡혀요"**
→ vendors.json 에서 지우거나 `until` 을 넣고 저장소에 올린다.
`until` 이 작년이면 올해는 점검 대상에서 통째로 빠진다.

**"1월에 돌렸더니 전 거래처가 확인 필요로 나와요"**
→ 옛 버전의 증상이다. 지금은 기한이 지난 결번이 하나도 없으면 잡지 않는다.
그래도 그렇게 나오면 홈택스 파일을 안 올렸거나 연도가 다른 파일일 가능성이 높다.
`check_input.py` 결과를 먼저 본다.

**"세금계산서는 왔는데 금액이 0이에요"**
→ 발행 후 수정세금계산서로 전액 취소된 것이다(노란 칸).
매입세액공제를 못 받는 상태라 재발행 요청이 필요하다. 가장 놓치기 쉬운 항목이다.

**"매트릭스에 처음 보는 회사가 잔뜩 있어요"**
→ `대상` 열이 `대상외` 인 행이다. 점검 대상이 아니라 참고용이다.

**"sync.py 가 404를 냅니다"**
→ 저장소가 비공개거나 파일이 아직 안 올라간 것이다. 저장소를 공개로 바꾸거나,
비공개로 두려면 읽기에도 토큰이 필요하므로 사용자에게 알린다.

---

## 코드를 고쳤을 때

**로컬에서만 고치고 끝내면 안 된다.** 다음 실행 때 `sync.py` 가 저장소 버전으로
덮어써서 수정이 사라진다. 반드시 저장소에 반영한다.

고친 뒤에는 반드시:

```bash
python3 tests/test_edge_cases.py
```

빈 데이터·1건·전액취소·연말/연초 실행·사업자 변경 등 실제로 조용히 틀렸던
케이스가 들어 있다. **전부 통과하지 않으면 저장소에 올리지 않는다.**

`validate.py` 의 "임계값 단일 출처" 검사도 함께 본다.
config 값을 코드에 하드코딩하는 순간 config 가 거짓말을 시작한다.

고친 뒤에는 `--code` 로 올린다. 안 올리면 다음 실행 때 sync 가 덮어쓴다.

```bash
python3 scripts/push.py <토큰> --code
```

`push.py` 가 테스트를 먼저 돌리고 실패하면 올리지 않으므로,
위 명령 하나로 '테스트 → 확인 → 반영'이 한 번에 처리된다.

---

## 저장소 구성

```
SKILL.md                   이 파일. **정본 설명서.** 설치 경로에 넣는 건 install/SKILL.md 다
README.md                  저장소 소개
sync.py                    재동기화용 (최초 받기는 curl+tar 한 줄로 한다)
config/check-config.json   임계값·색상·시트명 (단일 출처)
config/vendors.json        점검 대상 거래처 마스터
references/column-mapping.md   홈택스 파일 읽는 법과 그 이유
references/judgment-rules.md   등급 판정 논리
references/excel-format.md     산출물 서식 규격
scripts/common.py          config 로딩·파싱·상계 매칭
scripts/check_input.py     입력 사전 검증
scripts/build_report.py    리포트 생성
scripts/validate.py        산출물 검증
scripts/push.py            state·vendors·code 저장소 반영
state/last-run.json        직전 실행 결과 (자동 생성)
tests/                     엣지케이스 테스트 + 샘플 파일
audit/last-audit.md        직전 점검 기준선 (1단계에서 함께 읽는다)
install/SKILL.md           설치용 얇은 부트스트랩. 이걸 스킬에 재업로드한다
```
